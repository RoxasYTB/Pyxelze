# Documentation

Les fichiers de documentation (guides de build, notes de release, etc.) sont centralisés ici.

- `docs/` : documentation de projet (guides et notes)
- `scripts/` : scripts de build et utilitaires

Pour construire et créer le dossier `publish_final` :

```bash
# depuis la racine du projet
./scripts/rebuild_publish.sh
```
